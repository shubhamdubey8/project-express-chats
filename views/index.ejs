<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
    
  
    <style>
        body{
            background-image: url(https://images.unsplash.com/photo-1636751364472-12bfad09b451?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D);  
            background-size: cover;
        }
        h3{
            text-align: center;
        }
        form{
            
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }
        form button{
            border-radius: 15px;
            color: blue;
            
            cursor: pointer;
        }
        
        
    </style>
</head>
<body>
    <h3>ALL CHATS</h3>
    <form method="GET" action="/chats/new">
        <button class="btn">New chat</button>
    </form>
    <br>
    <% for (let chat of chats) { %>

       
        <div class="chat">
            <p>from: <%= chat.from %></p>
            <div class="msg">
                <p><%= chat.msg %></p>
                <form method="GET" action="/chats/<%= chat._id  %>/edit">
                    <button>edit</button>
                    <BR></BR>
                </form>
                <form method="POST" action="/chats/<%=chat._id  %>?_method=DELETE">
                    <button>DELETE</button>
                
                </form>
            </div>
            <p>Received by <%= chat.to %></p>
            <hr>
            <% if (chat.created_at) { %>
                <p><%= chat.created_at.toString().split(" ")[4] %></p>
                <p><%= chat.created_at.toString().split("-").splice(0,4).join(" ") %></p>
            <% } else { %>
                <p>No date available</p>
            <% } %>
        </div>
        <br>
        <% } %>
       
         
        
        
</body>
</html>
